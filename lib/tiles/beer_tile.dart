import 'package:flutter/material.dart';
import 'package:loja_virtual/datas/beer_data.dart';

class BeerTitle extends StatelessWidget {

  final String type;
  final BeerData data;

  BeerTitle(this.type, this.data);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
